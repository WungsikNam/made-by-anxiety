# Panic Zero Lite — 제품 기획서

> 버전: v2.0
> 작성일: 2026-02-18
> 상태: 개발 완료 (MVP)

---

## 1. 서비스 개요

### 한 줄 정의
> **공황이 오는 순간, 버튼 하나.**

공황 발작 발생 시 인지 부하를 최소화한 채 즉각적으로 개입하는 단독 실행형 도구.
치료 앱이 아니라 **응급 도구**다.

### 핵심 원칙
- 로그인 없음 — 공황 상태에서 계정 입력은 불가능하다
- 서버 없음 — 완전 오프라인 동작, 개인정보 수집 없음
- 단일 진입점 — SOS 버튼 하나로 전 세계 어떤 사용자도 사용 가능

---

## 2. 타겟 사용자

| 구분 | 내용 |
|------|------|
| 주 타겟 | 공황 발작 또는 심한 불안감을 경험하는 성인 (18~45세) |
| 사용 맥락 | 대중교통, 직장, 혼자 있는 공간 등 즉각적 도움이 필요한 모든 상황 |
| 기술 숙련도 | 스마트폰 사용에 익숙한 일반인 |
| 지역 | 글로벌 (현재 한국어, 추후 다국어) |

### 경쟁 제품 비교

| 제품 | 특징 | 차별점 |
|------|------|--------|
| Calm | 정기 명상 훈련 앱 | 즉시 개입 도구 아님 |
| Headspace | 습관 형성 중심 | 공황 발작 순간 UX 없음 |
| MindShift | 5-4-3-2-1 텍스트 제공 | 카메라 뷰파인더 그라운딩 없음 |
| **Panic Zero** | **즉시 개입 + 카메라 그라운딩** | **공황 발작 그 순간을 위한 전용 도구** |

---

## 3. 근거 기반 기법

### 생리적 한숨 (Physiological Sigh)
- 출처: Stanford Huberman Lab, 2023
- 원리: 코로 2번 들이쉬고(폐포 재팽창) → 길게 내쉬기(CO₂ 빠른 배출)
- 효과: 교감신경계 즉각 억제, 심박수 정상화
- 1사이클: 흡기 4s → 추가 흡기 1.5s → 날숨 6s = **11초**

### 5-4-3-2-1 그라운딩
- 감각 앵커링으로 현재 순간에 집중
- 공황 상태의 이인화(dissociation) 완화
- 카메라 뷰파인더를 통한 시각 그라운딩으로 효과 강화

---

## 4. 화면 구조 (IA)

```
앱 시작
    ↓
[SharedPreferences: onboarding_done 확인]
    ├─ false → OnboardingScreen (PageView 2페이지)
    │       페이지 1: 앱 소개 + 생리적 한숨 패턴 시연 애니메이션
    │       페이지 2: 카메라 권한 요청 + 개인정보처리방침
    │       ↓ (완료 → onboarding_done = true 저장)
    └─ true → HomeScreen
                    ↓ (SOS 탭)
            PanicModeScreen
            ├─ Phase 1: BreathingPhase
            │   생리적 한숨 (4s 흡기 → 1.5s 추가 → 6s 날숨)
            │   22초(최소 2사이클) 후 "다음" 버튼 활성화
            │
            ├─ Phase 2: GroundingTextPhase
            │   5-4-3-2-1 그라운딩 텍스트 탭 순환
            │   하단 "카메라로 주변 보기" 버튼
            │
            └─ Phase 3: CameraGroundingPhase
                    [권한 있음] CameraPreview 배경 + 시각 미션 3단계
                    [권한 없음] 색상 스캔 모드 (단색 배경 + 텍스트)
                    미션 완료 → "안정됐어요" 버튼
                    3분 경과 → 카메라 자동 종료 → Phase 2 복귀
            ↓ (언제든)
            "안정됐어요" → HomeScreen
```

---

## 5. 화면별 상세 명세

### 5-1. 온보딩 (OnboardingScreen)

| 항목 | 내용 |
|------|------|
| 진입 조건 | 최초 실행 (`onboarding_done = false`) |
| 이탈 조건 | 완료 탭 (`onboarding_done = true` 저장) |
| 뒤로가기 | 페이지 1: 앱 종료 / 페이지 2: 이전 페이지 이동 |
| 건너뛰기 | 없음 (의도적 설계) |

**페이지 1 — 앱 소개**
- 앱명 + 슬로건
- 생리적 한숨 패턴 시연 애니메이션 (실제 속도로 재생)
- 호흡 단계 레이블 표시 (코로 들이마세요 / 한 번 더 흡입 / 천천히 내쉬어요)
- 과학적 근거 설명 텍스트

**페이지 2 — 카메라 권한**
- 카메라 그라운딩 기능 설명
- "사진 촬영 없음 · 저장 없음 · 전송 없음" 명시
- 개인정보처리방침 링크 (다이얼로그)
- "시작하기" → 카메라 권한 요청 후 완료
- "카메라 없이 시작" → 권한 없이 완료 (색상 스캔 모드로 대체)

---

### 5-2. 홈 (HomeScreen)

| 항목 | 내용 |
|------|------|
| 진입 조건 | 온보딩 완료 / 세션 종료 |
| 이탈 조건 | SOS 탭 |

- 화면 전체를 채우는 빨간 원형 SOS 버튼
- 미세한 pulse 애니메이션 (0.97 ↔ 1.03, 2초 주기) — 살아있음을 느끼게
- "도와줘 / SOS" 텍스트
- 탭 시 0.94 스케일 다운 → PanicModeScreen으로 페이드 전환

---

### 5-3. Panic Mode (PanicModeScreen)

#### Phase 1 — 호흡 (BreathingPhase)

| 항목 | 내용 |
|------|------|
| 애니메이션 | TweenSequence (0.5→0.9→1.0→0.5) — 생리적 한숨 패턴 |
| 1사이클 | 11초 |
| 진행 조건 | 최소 22초(2사이클) 경과 후 "다음" 버튼 페이드 인 |
| 햅틱 | 단계 전환 시 진동 피드백 |
| 배경 오디오 | 빗소리 / 밤벌레 / 모닥불 선택 (좌상단) |
| 종료 | 우상단 "안정됐어요" (확인 다이얼로그) |

#### Phase 2 — 그라운딩 텍스트 (GroundingTextPhase)

| 항목 | 내용 |
|------|------|
| 텍스트 | 5-4-3-2-1 감각 앵커링 18개 문장 순환 |
| 전환 | 탭 → 다음 텍스트 (AnimatedSwitcher 페이드) |
| 배경 오디오 | Phase 1에서 이어서 재생 |
| 다음 진입 | 하단 "카메라로 주변 보기" 버튼 |

#### Phase 3 — 카메라 그라운딩 (CameraGroundingPhase)

**카메라 권한 있음**

| 미션 | 내용 |
|------|------|
| 미션 1 | 빨간색(또는 파란색) 물건을 하나 찾아서 바라보세요 |
| 미션 2 | 지금 눈앞에서 가장 큰 물건은 무엇인가요? |
| 미션 3 | 창문이나 문이 보이나요? 그쪽으로 시선을 옮겨보세요 |

- CameraPreview 전체 화면 배경
- 반투명 오버레이에 미션 텍스트 표시
- "찾았어요 →" 탭 → 다음 미션
- 미션 3 완료 → "잘 하셨어요 / 안정됐어요" 버튼

**카메라 권한 없음 (색상 스캔 모드)**

- 단색 다크 배경 (AppColors.panicBg 일관)
- "지금 방 안에서 파란색 물건을 3개 찾아보세요"
- "찾았어요 →" 탭 → 완료

**공통**
- 3분 경과 → 카메라 자동 종료 → Phase 2 복귀
- 좌상단 "뒤로" → Phase 2 복귀
- Panic Mode 중 카메라 권한 재요청 금지

---

## 6. 기술 스택

### 플랫폼
| 항목 | 내용 |
|------|------|
| 프레임워크 | Flutter 3.19.3 |
| 언어 | Dart 3.3.1 |
| 지원 플랫폼 | iOS / Android / Web (PWA) |
| 화면 고정 | 세로 모드 고정 (`portraitUp`) |

### 패키지

| 패키지 | 용도 | 버전 |
|--------|------|------|
| just_audio | 배경 오디오 재생 | ^0.9.36 |
| wakelock_plus | Panic Mode 중 화면 켜짐 유지 | ^1.1.4 |
| shared_preferences | 온보딩 완료 플래그 저장 | ^2.2.2 |
| camera | 카메라 뷰파인더 (Phase 3) | ^0.11.0 |
| permission_handler | 카메라 권한 요청/확인 | ^11.3.1 |

### 상태 관리
- Flutter 내장 `setState` (외부 상태 관리 라이브러리 없음)
- Phase 전환: `enum _PanicPhase { breathing, groundingText, cameraGrounding }`
- 라우트 스택 추가 없이 `switch` 분기로 처리

### 오디오 에셋
| 파일 | 내용 | 크기 |
|------|------|------|
| `assets/audio/rain.mp3` | 빗소리 | ~5.1MB |
| `assets/audio/cricket.mp3` | 밤벌레소리 | ~4.8MB |
| `assets/audio/fire.mp3` | 모닥불소리 | ~4.6MB |

---

## 7. 디자인 시스템

### 색상 (AppColors)

| 토큰 | 헥스 | 용도 |
|------|------|------|
| `sos` | `#E8534A` | SOS 버튼, 강조 |
| `panicBg` | `#000000` | Panic Mode 배경 |
| `mainBg` | `#1A1A1A` | 홈 배경 |
| `breathCircle` | — | 호흡 원 메인 |
| `breathCircleGlow` | — | 호흡 원 글로우 |
| `textSecondary` | — | 보조 텍스트 |

### 타이포그래피 (AppTextStyles)
| 토큰 | 용도 |
|------|------|
| `sosLabel` | "도와줘" 버튼 텍스트 |
| `breathInstruction` | 호흡 단계 레이블 |
| `groundingText` | 그라운딩 텍스트 본문 |
| `groundingHint` | "탭해서 다음" 힌트 |

### 듀레이션 (AppDurations)
| 토큰 | 값 | 용도 |
|------|-----|------|
| `tapResponse` | 150ms | 버튼 탭 피드백 |
| `screenTransition` | 250ms | 화면 전환 페이드 |
| `groundingFade` | — | 텍스트 전환 |

### 반응형 레이아웃
- `.clamp(min, max)` 패턴으로 모바일~태블릿 대응
- SOS 버튼: 너비 72%, 최대 360px (세로) / 높이 60%, 최대 340px (가로)
- 호흡 원: 너비 60%, 최대 280px

---

## 8. 개인정보 & 보안

- 수집 데이터: **없음**
- 카메라: 뷰파인더 표시 전용, 촬영·저장·전송 없음
- 서버: 없음 (완전 로컬 동작)
- 분석 SDK: 없음

---

## 9. 향후 로드맵

### v2.1 (단기)
- [ ] 다국어 지원 (영어, 일본어)
- [ ] 앱 아이콘 / 스플래시 스크린 디자인
- [ ] App Store / Play Store 출시

### v3.0 Pro (중기)
- [ ] 그라운딩 미션 팩 추가 구매
- [ ] 호흡 패턴 커스텀 (박스 호흡 등)
- [ ] Apple Watch 연동 (심박수 기반 자동 실행)
- [ ] 세션 기록 (횟수, 지속시간) — 로컬 저장만

### 비즈니스 모델
- Lite: 무료 (현재)
- Pro: 일회성 구매 또는 연간 구독 (향후)

---

## 10. 제약 사항

| 항목 | 내용 |
|------|------|
| 의료 면책 | 이 앱은 의료 기기가 아니며 전문 치료를 대체하지 않습니다 |
| 임상 자문 | 없음 (공개된 연구 기반 자가 활용 도구) |
| 카메라 (Web) | 웹 환경에서는 색상 스캔 모드로 대체 |
| 오프라인 | 완전 오프라인 지원 (오디오 에셋 번들 포함) |

---

*Panic Zero Lite v2 — 기획·개발: 2026-02-18*
